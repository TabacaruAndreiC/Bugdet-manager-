import { Employee } from '../employee/employee.model';

export class WorkSession {
  lastName!: string;
  firstName!: string;
  hourRate!: number;
  hoursWorkedTotal!: number;
  date!: Date;
  projectName!: string;
  taskName!: string;
  hoursWorked!: number;
}
